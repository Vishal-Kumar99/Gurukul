<Application x:Class="Gurukul.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Gurukul"
             xmlns:viewModel="clr-namespace:Gurukul.MVVM.ViewModels"
             xmlns:view="clr-namespace:Gurukul.MVVM.Views"
             xmlns:viewModelAdmission="clr-namespace:Gurukul.MVVM.ViewModels.Admission"
             xmlns:viewAdmission="clr-namespace:Gurukul.MVVM.Views.Admission">
    <Application.Resources>
        <DataTemplate DataType="{x:Type TypeName=viewModel:HomeViewModel}">
            <view:HomeView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:SettingViewModel}">
            <view:SettingView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModelAdmission:AdmissionWizardViewModel}">
            <viewAdmission:AdmissionWizardView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type TypeName=viewModel:AddClassViewModel}">
            <view:AddClassView />
        </DataTemplate>
    </Application.Resources>
</Application>
