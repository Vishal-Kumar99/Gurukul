<Window x:Class="Gurukul.MVVM.Views.AcademicYearView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gurukul.MVVM.Views"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Background="Transparent"
        BorderBrush="Transparent"
        BorderThickness="0"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" Height="400" Width="700">
    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4338CA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3730A3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#E5E7EB"/>
            <Setter Property="Foreground" Value="#111827"/>
        </Style>

        <!-- Toggle Checkbox -->
        <Style x:Key="ToggleCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Width="42" Height="22">
                            <Border x:Name="Switch"
                                    CornerRadius="11"
                                    Background="#D1D5DB"/>
                            <Ellipse x:Name="Thumb"
                                     Width="18" Height="18"
                                     Fill="White"
                                     Margin="2"
                                     HorizontalAlignment="Left"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Switch" Property="Background" Value="#4F46E5"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    
    <Border CornerRadius="16" Background="white" BorderBrush="#303030" BorderThickness="2" Padding="20" 
            MouseDown="Window_MouseDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid>
                <TextBlock Text="Academic Year"
                            FontSize="22"
                            FontWeight="Bold"
                            Foreground="#111827"/>

                <Button Content="✕"
                            HorizontalAlignment="Right"
                            Width="32"
                            Height="32"
                            FontSize="14"
                            Background="#F3F4F6"
                            BorderThickness="0"
                            Cursor="Hand"
                            Command="{Binding CloseWindowCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            </Grid>

            <Grid Grid.Row="1" Margin="0 20 0 0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Border CornerRadius="12" Background="#F9FAFB" Padding="15">
                    <ListView BorderThickness="0" ScrollViewer.CanContentScroll="True" ItemsSource="{Binding SessionList}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Session" Width="150" DisplayMemberBinding="{Binding YearName}"/>
                                <GridViewColumn Header="Session Status" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay}"
                                                        HorizontalAlignment="Center"
                                                        Style="{StaticResource ToggleCheckBoxStyle}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Border>

                <Border Grid.Column="1"
                        Margin="20,0,0,0"
                        CornerRadius="12"
                        Background="#F9FAFB"
                        Padding="20"
                        Width="240">
                    <StackPanel>
                        <TextBlock Text="Add Session"
                                    FontSize="16"
                                    FontWeight="Bold"
                                    Margin="0,0,0,15"/>
                        <TextBlock Text="Start Date" Foreground="#6B7280"/>
                        
                        <DatePicker SelectedDate="{Binding SelectedStartYear}" Margin="0 5 0 20"/>

                        <Button Content="Add Session"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Height="36"
                                Command="{Binding AddSessionCommand}"/>

                        <Button Content="Close"
                                Margin="0 10 0 0"
                                Height="36"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding CloseWindowCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
