<UserControl x:Class="Gurukul.MVVM.Views.Admission.Steps.AcademicDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gurukul.MVVM.Views.Admission.Steps"
             mc:Ignorable="d" 
             Background="#F3F4F6"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Width="400">

            <!-- Previous School -->
            <TextBlock Text="Previous School Name" />
            <TextBox Text="{Binding PreviousSchool, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Last Class -->
            <TextBlock Text="Last Class Studied" Margin="0,10,0,0"/>
            <TextBox Text="{Binding LastClassStudied, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Board -->
            <TextBlock Text="Board / Medium" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Board, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Result -->
            <TextBlock Text="Result / Grade" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Result, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Remarks -->
            <TextBlock Text="Remarks" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     Height="60"/>

            <!-- Validation Errors -->
            <ItemsControl ItemsSource="{Binding Errors}" Margin="0,12,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Red" FontSize="12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
