<UserControl x:Class="Gurukul.MVVM.Views.Admission.Steps.FinalSubmitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gurukul.MVVM.Views.Admission.Steps"
             xmlns:convert="clr-namespace:Gurukul.Converters"
             mc:Ignorable="d" 
             Background="#F3F4F6"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <convert:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Width="500">

            <TextBlock Text="Confirm Admission Details"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>

             <!--Admission No--><!--
            <TextBlock Text="Admission Number" FontWeight="Bold" />
            <TextBlock Text="{Binding Draft.AdmissionNo, UpdateSourceTrigger=PropertyChanged}" />-->

            <!-- Student -->
            <TextBlock Text="Student" FontWeight="Bold"/>
            <TextBlock Text="{Binding Draft.FullName}" />
            <TextBlock Text="{Binding AdmissionNo}" />

            <!-- Parent -->
            <TextBlock Text="Parent" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Draft.FatherName}" />
            <TextBlock Text="{Binding Draft.MobileNo}" />

            <!-- Address -->
            <TextBlock Text="Address" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Draft.AddressLine}" />
            <TextBlock Text="{Binding Draft.City}" />

            <!-- Health -->
            <TextBlock Text="Health" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Draft.BloodGroup}" />

            <!-- Photo Preview -->
            <TextBlock Text="Photo" FontWeight="Bold" Margin="0,10,0,0"/>
            <Border Width="120" Height="150"
                    BorderBrush="#9CA3AF"
                    BorderThickness="1">
                <Image Source="{Binding Draft.StudentPhoto,
                                Converter={StaticResource ByteArrayToImageConverter}}"
                       Stretch="UniformToFill"/>
            </Border>

            <TextBlock Text="Click Finish to complete admission."
                       Margin="0,12,0,0"
                       Foreground="#6B7280"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
