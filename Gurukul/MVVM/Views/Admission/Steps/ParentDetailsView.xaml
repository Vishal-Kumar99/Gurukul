<UserControl x:Class="Gurukul.MVVM.Views.Admission.Steps.ParentDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gurukul.MVVM.Views.Admission.Steps"
             mc:Ignorable="d" 
             Background="#F3F4F6"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Width="400">

            <!-- Father Name -->
            <TextBlock Text="Father Name" />
            <TextBox Text="{Binding FatherName, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Mother Name -->
            <TextBlock Text="Mother Name" Margin="0,10,0,0"/>
            <TextBox Text="{Binding MotherName, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Mobile -->
            <TextBlock Text="Mobile Number" Margin="0,10,0,0"/>
            <TextBox Text="{Binding MobileNo, UpdateSourceTrigger=PropertyChanged}"
                     MaxLength="10" />

            <!-- Email -->
            <TextBlock Text="Email" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Validation Errors -->
            <ItemsControl ItemsSource="{Binding Errors}" Margin="0,12,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Red" FontSize="12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>
