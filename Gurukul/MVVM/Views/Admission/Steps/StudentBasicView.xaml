<UserControl x:Class="Gurukul.MVVM.Views.Admission.Steps.StudentBasicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gurukul.MVVM.Views.Admission.Steps"
             mc:Ignorable="d" 
             Background="#F3F4F6"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Width="400">

            <!-- Student Name -->
            <TextBlock Text="Full Name" Margin="0,10,0,0"/>
            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />

            <!-- Class -->
            <TextBlock Text="Class" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding SortedClassView}"
                      DisplayMemberPath="ClassName"
                      SelectedValuePath="ClassId"
                      SelectedValue="{Binding ClassId}" />

            <!-- Section -->
            <TextBlock Text="Section" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding SortedSectionView}"
                      DisplayMemberPath="SectionName"
                      SelectedValuePath="SectionId"
                      SelectedValue="{Binding SectionId}" />

            <!-- Validation Errors -->
            <ItemsControl ItemsSource="{Binding Errors}" Margin="0,12,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Red" FontSize="12"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>

</UserControl>
