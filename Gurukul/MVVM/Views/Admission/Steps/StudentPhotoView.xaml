<UserControl x:Class="Gurukul.MVVM.Views.Admission.Steps.StudentPhotoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gurukul.MVVM.Views.Admission.Steps"
             mc:Ignorable="d" 
             Background="#F3F4F6"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20" Width="400">

        <TextBlock Text="Upload Student Photo"
                   FontSize="16"
                   FontWeight="SemiBold"/>

        <!-- Preview -->
        <Border Width="150" Height="180"
                BorderBrush="#9CA3AF"
                BorderThickness="1"
                CornerRadius="4"
                Margin="0,10,0,0">
            <Image Source="{Binding PhotoPreview}"
                   Stretch="UniformToFill"/>
        </Border>

        <!-- Browse Button -->
        <Button Content="Browse Photo"
                Width="140"
                Margin="0,10,0,0"
                Command="{Binding BrowsePhotoCommand}" />

        <!-- Validation Errors -->
        <ItemsControl ItemsSource="{Binding Errors}" Margin="0,12,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" Foreground="Red"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="Allowed formats: JPG, PNG"
                   Foreground="#6B7280"
                   FontSize="12"
                   Margin="0,8,0,0"/>
    </StackPanel>
</UserControl>
