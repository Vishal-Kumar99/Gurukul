<Window x:Class="Gurukul.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gurukul"
        xmlns:components="clr-namespace:Gurukul.Components"
        mc:Ignorable="d"
        Title="GPS" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <components:Sidebar Grid.Column="0" DataContext="{Binding SidebarVM}" />
        
        <ContentControl Grid.Column="1" Content="{Binding Navigation.CurrentView}"/>
    </Grid>
</Window>
